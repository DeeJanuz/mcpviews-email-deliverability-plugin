# Unreleased

- **feat**: Split the public plugin surface into Email Campaigns and Email Performance manifests at `0.2.0`.
- **fix**: Build and release `email-campaigns.zip` and `email-performance.zip` so split manifest downloads resolve correctly.
